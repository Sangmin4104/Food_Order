<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="InsertCartMapper">

    <insert id="InsertCart" parameterType="com.example.ysu.model.dto.InsertCartDTO">
        insert into cart (u_id, menu_id) VALUES (#{u_id}, #{menu_id})
    </insert>

</mapper>